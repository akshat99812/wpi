"use client";

import React from 'react';
import ChatBot from '@/components/Engines/ChatBot';

/**
 * Research → Chatbot tab. The Pro RAG assistant over 25 years of Indian
 * wind-energy directories. The sidebar (in the research layout) switches
 * between this, Intelligence, and Policy.
 */
export default function ResearchChatbotPage() {
  return <ChatBot />;
}
